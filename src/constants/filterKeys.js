const FILTER_KEYS = {
  id: 'id',
  name: 'name',
  date: 'date',
  title: 'title',
  field: 'field',
  old_value: 'old_value',
  new_value: 'new_value',
};

Object.preventExtensions(FILTER_KEYS);
export default FILTER_KEYS;
