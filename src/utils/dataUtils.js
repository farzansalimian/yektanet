export const getItemId = (item) => item?.id;
export const getItemName = (item) => item?.name;
export const getItemDate = (item) => item?.date;
export const getItemTitle = (item) => item?.title;
export const getItemField = (item) => item?.field;
export const getItemOldValue = (item) => item?.old_value;
export const getItemNewValue = (item) => item?.new_value;
